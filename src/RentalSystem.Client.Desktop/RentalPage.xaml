<Page x:Class="RentalSystem.Client.Desktop.RentalsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:conv="clr-namespace:RentalSystem.Client.Desktop.Converters"
      mc:Ignorable="d"
      Title="Zarządzanie Wypożyczeniami">

    <Page.Resources>
        <conv:StatusToColorConverter x:Key="StatusColorConverter"/>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="All Rentals" 
                       FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="Refresh" 
                    Click="Refresh_Click"
                    Margin="20,0,0,0" Padding="10,5"/>
        </StackPanel>

        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Rentals}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  BorderThickness="1" 
                  BorderBrush="#DDDDDD"
                  RowHeaderWidth="0">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Background" Value="{Binding Status, Converter={StaticResource StatusColorConverter}}"/>
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="TextAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="From" Binding="{Binding StartDate, StringFormat=dd.MM.yyyy}" Width="90"/>
                <DataGridTextColumn Header="To" Binding="{Binding EndDate, StringFormat=dd.MM.yyyy}" Width="90"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat={}{0:C}}" Width="80"/>

                <DataGridTextColumn Header="Item ID" Binding="{Binding ItemId}" Width="120"/>
                <DataGridTextColumn Header="OwnerId" Binding="{Binding OwnerId}" Width="120"/>
                <DataGridTextColumn Header="BorrowerId" Binding="{Binding BorrowerId}" Width="120"/>

                <DataGridTemplateColumn Header="Actions" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Cancel rental" 
                                    Command="{Binding DataContext.CancelRentalCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                    CommandParameter="{Binding}"
                                    Background="IndianRed" Foreground="White"
                                    Padding="10,2" Margin="5"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>